<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.itwillbs.mappers.memberMapper">

  <select id="userCheck" 
                  resultType="com.itwillbs.domain.MemberDTO">
    select * from members 
    where mem_id = #{mem_id} and mem_pass = #{mem_pass}
  </select> 
  
   <select id="getMember" 
                  resultType="com.itwillbs.domain.MemberDTO">
    select * from members 
    where mem_id = #{mem_id}
  </select> 
  
  <select id="getMemberList" 
                  resultType="com.itwillbs.domain.MemberDTO">
    select * from members 
  </select> 
  
  
<!--   com.itwillbs.mappers.memberMapper.insertMember -->
<!--   => sql이름 호출해서 사용  
       => ? 자리 MemberDTO 멤버변수 #{이름}-->
  <insert id="insertMember">
  	insert into members(mem_id ,mem_pass ,mem_name ,mem_phone ,mem_gender ,mem_confirm
  						,mem_file ,mem_add, mem_indate) 
  						
  	values(#{mem_id},#{mem_pass},#{mem_name},#{mem_phone},
  			#{mem_gender},#{mem_confirm},#{mem_file},#{mem_add},#{mem_indate})
  </insert>
  
  <update id="updateMember">
  	update members 
  	set name = #{name} 
  	where id = #{id}
  </update>
  
  <delete id="deleteMember">
  	delete from members 
  	where id = #{id}
  </delete>
  
</mapper>